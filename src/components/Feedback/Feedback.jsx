import style from "./Feedback.module.scss"
const Feedback = () => {
    return (
        <section className={style.wrapper}>
            <h5 className={style.title}>Відклики</h5>
        </section>
      );
}
 
export default Feedback